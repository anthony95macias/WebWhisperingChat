import { AppBar, Toolbar } from '@mui/material'

const Header = () => {
  return (
    <AppBar sx ={{}}>
        <Toolbar>
            <p>home</p>
        </Toolbar>
    </AppBar>
  )
}

export default Header
